<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright (c) 2020. Está classe está sendo consedida para uso pessoal
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.BiblioLivro.criarlivros">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:icon="@drawable/iconapp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconapp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="true"
        android:allowBackup="true">

        <activity android:name=".activities.SplashScreen"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.TelaPrincipal"
            android:label="@string/app_name"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".activities.TelaCadastrar"
            android:label="@string/title_activity_tela_cadastrar"
            android:screenOrientation="fullSensor"
            android:parentActivityName=".activities.TelaPrincipal">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.TelaPrincipal" />
        </activity>

        <activity android:name=".activities.TelaPesquisar"
            android:label="@string/title_activity_tela_pesquisar"
            android:parentActivityName=".activities.TelaPrincipal">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.TelaPrincipal" />
        </activity>

        <activity android:name=".activities.TelaImpostacoes"
            android:label="@string/menu_settings"
            android:parentActivityName=".activities.TelaPrincipal"
            android:screenOrientation="fullSensor">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.TelaPrincipal"/>
        </activity>

    </application>

</manifest>